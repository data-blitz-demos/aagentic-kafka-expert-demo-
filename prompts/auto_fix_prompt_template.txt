A user clicked a `Fix:` action produced by Kafka Monitor Agent. This is the exact A2A handoff to Kafka Remediation Agent.

Issue line:
$issue_line

BEGIN MONITOR DIAGNOSIS (REQUIRED CONTEXT — PRESERVE VERBATIM IN YOUR PLAN)
$monitor_reason
END MONITOR DIAGNOSIS

BEGIN COMPLETE MONITOR EVIDENCE (READ-ONLY CONTEXT)
$monitor_context
END COMPLETE MONITOR EVIDENCE

Objective:
- Independently verify and remediate only this issue using all available tools:
  - Prometheus MCP telemetry tools
  - Kafka Admin MCP tools (including mutation tools)
  - Schema Registry MCP inventory tools

Rules:
1) Authorization depends on the Doer mode:
   - Human-in-the-Loop: use this message first to draft a non-mutating plan. Execute tools only after the human selects `Approve Plan & Run Doer`.
   - Automatic execution: the user's `Fix:` click authorizes execution for this issue.
2) If mutation is required, perform the smallest safe change and then verify with read tools.
3) Report concrete evidence after the change attempt (metrics/metadata values).
4) If full remediation is not possible with available tools/access, explain exactly what remains.
5) Output only 1-based numbered bullets with `[GOOD]`, `[WARN]`, `[BAD]`.
6) Include `Fix:` only for unresolved warning/bad bullets.
7) Keep response concise and operational.
8) Do not treat approval as a requirement to mutate. If read evidence does not justify a change, report that clearly and leave the cluster unchanged.
9) Begin the response by naming the Monitor-reported diagnosis exactly (or quote it after `Diagnosis:`), then state whether live evidence confirms it. Never omit or replace this diagnosis with only the `Fix:` action.
10) Use Neo4j Graph RAG for relevant failure-mode or remediation knowledge, while using live tools as the authority for current cluster state.
